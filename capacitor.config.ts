import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Market At The Lawn',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
